# Локальные прогоны — не коммитить по умолчанию
runs/*/
!runs/.gitkeep

latest/*
!latest/README.md

# Ручные черновики (раскомментируйте если нужно коммитить протоколы)
# manual/*.md
# !manual/.gitkeep
