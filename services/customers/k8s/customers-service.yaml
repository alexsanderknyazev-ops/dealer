apiVersion: v1
kind: Service
metadata:
  name: customers-service
  namespace: dealer
spec:
  selector:
    app: customers-service
  ports:
    - name: grpc
      port: 50052
      targetPort: 50052
    - name: http
      port: 8081
      targetPort: 8081
