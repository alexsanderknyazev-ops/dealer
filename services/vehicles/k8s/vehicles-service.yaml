apiVersion: v1
kind: Service
metadata:
  name: vehicles-service
  namespace: dealer
spec:
  selector:
    app: vehicles-service
  ports:
    - name: grpc
      port: 50053
      targetPort: 50053
    - name: http
      port: 8082
      targetPort: 8082
